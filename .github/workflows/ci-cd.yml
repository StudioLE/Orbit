name: CI/CD

on:
  push:

jobs:

  analysis:
    name: Analysis
    uses: StudioLE/Actions/.github/workflows/01-semantic-release.yml@main
    secrets: inherit

  build-and-test-ubuntu:
    name: Ubuntu
    needs: analysis
    uses: StudioLE/Actions/.github/workflows/02-build-and-test.yml@main
    with:
      solution: ""
      os: ubuntu-latest
      version: ${{ needs.analysis.outputs.version }}
    secrets: inherit

  build-and-test-windows:
    name: Windows
    if: ${{ needs.analysis.outputs.version != '0.0.0' }}
    needs: analysis
    uses: StudioLE/Actions/.github/workflows/02-build-and-test.yml@main
    with:
      solution:  ""
      os: windows-latest
      version: ${{ needs.analysis.outputs.version }}
    secrets: inherit

  publish-nuget:
    name: Publish NuGet
    if: ${{ needs.analysis.outputs.version != '0.0.0' }}
    needs:
      - analysis
      - build-and-test-ubuntu
    uses: StudioLE/Actions/.github/workflows/03-publish-nuget.yml@main
    with:
      version: ${{ needs.analysis.outputs.version }}
    secrets: inherit
